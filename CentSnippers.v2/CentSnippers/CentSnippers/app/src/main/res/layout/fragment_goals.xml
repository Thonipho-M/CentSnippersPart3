<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/goalsFragmentLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- RecyclerView to display list of goals -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/goalsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/addGoalFab"
        android:clipToPadding="false"
        android:paddingBottom="72dp" />

    <!-- Floating action button to add a new goal -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addGoalFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        app:backgroundTint="@color/purple_500"
        app:tint="@android:color/white"
        android:contentDescription="Add Goal"
        app:srcCompat="@android:drawable/ic_input_add" />
</RelativeLayout>
